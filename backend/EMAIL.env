# 是否啟用郵件功能
EMAIL_ENABLED="true"

# 發送方式選擇
USE_SMTP="true"  # 設為 "true" 使用 SMTP（推薦，更簡單），設為 "false" 使用 Gmail API（需要 credentials.json）

# Gmail 帳號設定
SMTP_USER="si.pingtai@gmail.com"       # Gmail 信箱（寄件人）
SMTP_FROM_NAME="智慧實習平台"           # 寄件人名稱

# SMTP 設定（如果 USE_SMTP="true"）
SMTP_HOST="smtp.gmail.com"              # SMTP 伺服器
SMTP_PORT="587"                         # SMTP 埠號
                                        # 如果 587 被阻擋，可嘗試：
                                        # - 465 (SSL，需要修改代碼使用 SMTP_SSL)
                                        # - 25 (不推薦，可能不支援 TLS)
                                        # 建議：如果持續無法連線，改用 Gmail API (USE_SMTP="false")
SMTP_PASSWORD="wage ysfw mihg kpur"     # Gmail 應用程式密碼（必須設定！）
                                        # 取得方式：
                                        # 1. 前往 https://myaccount.google.com/
                                        # 2. 安全性 → 兩步驟驗證（需先啟用）
                                        # 3. 應用程式密碼 → 產生新的應用程式密碼
                                        # 4. 複製 16 位密碼並填入此處（例如：abcd efgh ijkl mnop）

# Gmail API 設定（如果 USE_SMTP="false"）
GMAIL_CREDENTIALS_PATH="credentials.json" 
GMAIL_TOKEN_PATH="token.json"
