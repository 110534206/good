# 是否啟用郵件功能
EMAIL_ENABLED="true"

EMAIL_METHOD="smtp"

# SMTP 設定（使用應用密碼方式）
SMTP_USER="si.pingtai@gmail.com"        # Gmail 信箱
SMTP_PASSWORD="stfq grxr maoy hcwo"                         # Gmail 應用密碼（16位數字，從 https://myaccount.google.com/apppasswords 取得）
SMTP_FROM_NAME="智慧實習平台"           # 寄件人名稱
SMTP_HOST="smtp.gmail.com"              # Gmail SMTP 伺服器
SMTP_PORT="587"                          # Gmail SMTP 端口（587 for TLS）

# Gmail API 設定（僅在使用 gmail_api 方式時需要）
GMAIL_CREDENTIALS_PATH="credentials.json" 
GMAIL_TOKEN_PATH="token.json"
